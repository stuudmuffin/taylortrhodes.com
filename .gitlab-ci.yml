image: docker:latest

before_script:
    - docker info

build_image:
    script:
        - docker build -t c_nginxphp .
        - docker run c_nginxphp
    artifacts:
        paths:
        - public

